name: Redesign Preview Validation

on:
  push:
    branches:
      - portfolio-redesign
  pull_request:
    branches:
      - master

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Build portfolio
        run: npm run build

      - name: Run redesign browser checks
        run: npm run test:redesign

      - name: Upload redesign screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: redesign-browser-screenshots
          path: |
            test-results/*.png
            playwright-report/
          if-no-files-found: warn
          retention-days: 7
